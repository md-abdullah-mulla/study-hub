# GitHub Pages build (see package.json → build:pages)
# Runs the whole backend — SQLite as WebAssembly — inside the browser,
# so the app works without any server. Data lives in IndexedDB.
VITE_API_MODE=local
