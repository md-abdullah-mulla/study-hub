# Vercel build (see package.json → build:vercel and vercel.json)
# Exactly like the GitHub Pages build the whole backend — SQLite as WebAssembly —
# runs inside the browser, so no server or database hosting is needed.
VITE_API_MODE=local
