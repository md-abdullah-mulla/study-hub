# Desktop app build (see package.json → build:desktop and desktop/)
# Like the web app builds, the whole backend — SQLite as WebAssembly — runs
# inside the app, so the desktop app needs no server and works offline.
VITE_API_MODE=local
